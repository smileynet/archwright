# Ephemeral working notes
.scratch/

# Reference repos (large, not project source)
.references/
__pycache__/
.scratch/

# Generated at deploy time by tools/deploy-skills.sh (sources of truth:
# tools/domains/, tools/stacks/REGISTRY.yaml, docs/glossary.md). With global
# kiro symlink deploys these materialize into the source tree through the link.
skills/archwright-survey/references/domains/
skills/archwright-survey/references/stacks/
skills/archwright-survey/references/glossary.md

# Machine-local real-name denylist for sanitize-check (real<->alias map lives in recall, never in the repo)
.sanitization-denylist
