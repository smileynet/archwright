---
kind: force
id: force-slug
polarity: desire            # desire | constraint
# hardness: hard            # required for constraints: hard (physics/security/givens) | soft (preference/convention)
evidence_level: L3          # L1 stated by human … L5 inferred by agent (see archwright-forces)
source: ""                  # where this force came from: grill:Q03, adr:0002, domain rule, README
serves: []                  # constraints only: bare ids of product desires this constraint serves
---

# Force Name

## Statement

One sentence. Desires state what someone wants ("Any fielder can receive a pass at any time"). Constraints state what bounds the design ("Exactly one entity holds the ball").

## Who Feels It

The user/role/system that experiences this force. Desires trace to humans; constraints trace to the world (physics, law, platform) or to prior decisions.

## Evidence

Why we believe this force is real — quotes, domain rules, observed behavior. Higher evidence → higher confidence in patterns that resolve it.
