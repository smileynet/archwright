# Keep skills and tools in lockstep — grep the tool for any field a skill mandates

One-line: derive skill mandated `target_status: pending`; check.py had never implemented it → 20+ pending specs reported FAIL.

**Date:** 2026-07-16 · **Source:** DemoAR pipeline run

When a skill documents a spec field, the tool consuming that spec must handle
it. Audit direction: for every field a skill tells authors to write, grep the
tools for a reader.
